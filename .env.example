# Supabase（必須）
# https://supabase.com/dashboard でプロジェクト作成後、設定から取得
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# サーバー側のみ（RLS をバイパスする処理などで必要になる場合）
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# NextAuth（必須）
# https://generate-secret.vercel.app/ 等で生成
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret

# メール送信（Resend）
# https://resend.com/api-keys で API キーを取得。未設定の場合は送信をスキップし履歴も記録しない
RESEND_API_KEY=
# 送信元アドレス（Resend で検証済みドメイン、または onboarding@resend.dev 等）
MAIL_FROM=ちよプラブックスペース <onboarding@resend.dev>
